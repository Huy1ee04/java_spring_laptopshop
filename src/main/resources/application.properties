#File c?u hinh c?a các thu?c tính c?n dùng
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/nhasach
spring.datasource.username=root
spring.datasource.password=Huy2004@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true

#config spring security
spring.security.user.name=hoidanit
spring.security.user.password=123456

#default = 1MB
spring.servlet.multipart.max-file-size=50MB
#default = 10 MB (form data)
spring.servlet.multipart.max-request-size=50MB

#config session
spring.session.store-type=jdbc
spring.session.timeout=30m
spring.session.jdbc.initialize-schema=always

#debug=true

##config view engine (jsp)
#spring.mvc.view.prefix = /WEB-INF/view/
## t? ??ng thêm h?u t? (.jsp) cho tên file
#spring.mvc.view.suffix = .jsp
# Khi ?ã có folder config riêng thì không c?n ph?i làm theo cách này